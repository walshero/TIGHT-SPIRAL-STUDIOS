# THE PRIVACY FLOOR — Tight Spiral Productions
# Written 2026-07-14. CORRECTED the same day, by the founder.
#
# EVERY FILE IN THIS REPO IS A PUBLIC URL. GitHub Pages serves the whole tree.
# A file pushed here is a file published. This list is the only tier there is.
#
# ⚠ WHAT IS **NOT** PRIVATE — and a machine keeps getting this wrong:
#   - MassBay faculty names and @massbay.edu emails = PUBLIC DIRECTORY INFO.
#   - Student writing PUBLISHED IN LUMIÈRE (lumierelitmag.com) = PUBLISHED WORK.
#     Attribution is the POINT. A litmag that hides its writers is not a litmag.
#   Confluence's faculty directory and its published-student showcase are FEATURES.
#   On 2026-07-14 an over-broad rule flagged these as a FERPA CRITICAL and pulled
#   the canon trunk off the live site. That was WRONG and was reverted.
#   A guard that cries wolf is worse than no guard: it trains the founder to
#   ignore it, which disables every real finding it will ever make.
#
# ⚠ WHAT **IS** PRIVATE: unpublished student work, grades, enrollment records,
#   advising/disciplinary notes, anything out of a gradebook or an SIS.

# --- PIPELINE IP. The Borges paper is ABOUT this. Do not publish it for free. ---
claude-project-instructions.md
chatgpt-pro-instructions.md
*-pro-instructions.md

# --- CLIENT WORK. Their business logic is theirs, not the public's. ---
private/

# --- ACTUAL student records (NOT published work) ---
*gradebook*
*-grades-*
*enrollment*
*advising-notes*

# --- SECRETS ---
*.pat
.env
*token*

# --- container junk ---
__pycache__/
*.pyc
