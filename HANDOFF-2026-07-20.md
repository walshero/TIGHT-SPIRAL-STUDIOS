# HANDOFF — 2026-07-20 (fidelity + Menard + Borges quotes)

## WHAT SHIPPED THIS SESSION (all repo-verified against git tree, not CDN)

1. **Content-fidelity gate system** — closes the scene/game coherence drift.
   - `tsp-intake.html` — front door. Collects the three named things (IMAGE / VERB / SOURCE) + decisions-per-screen, renders the brief back (HITL), emits `brief.json`. Gate exit 0, worst pair 11.19, AAA. Repo commit `74dba07`.
   - `os-block-fidelity-gate.md` — the OS block. Why-this-exists, three named things, spoken check (4 NOW), the way (preship-fidelity-gate.py spec), doctrine fit. Repo commit `7d31172`.

2. **Menard doctrine + teeth** (the reskin-as-original claim).
   - OS §1 framing + `menard_check` tooth in `os-block-fidelity-gate.md`.
   - Intake form "Is this a reskin?" block: `reskin_of` + `what_changed`. A named parent with blank what_changed renders the PHOTOCOPY HALT. Live in `tsp-intake.html`.

3. **Borges paper** — `rescued/borges-pipeline-paper.md`, repo commit `e6b5c7e`, git-verified.
   - Menard section added ("## Menard: the copy that isn't").
   - Six attributed Borges quotes, one per section, OPERATIONAL not scenery: garden = "divergent, convergent and parallel times"; library = "thousands of false catalogues"; Funes = "to forget differences, generalize, make abstractions"; Aleph = "simultaneous ... successive"; map = "to the rigours of sun and rain"; Menard = "almost infinitely richer".
   - All 56 em-dashes removed (converted to commas). Header note flipped to declare quotes present + fair-use + verify-translation.

## PENDING — MATT ONLY

- **Verify the six Borges quotes** against your preferred edition before submission. Operational lines cut deeper into translation; wording varies edition to edition. Flagged in header note + HITL item (2).
- **Revoke both burned GitHub PATs** (pasted in a prior session, visible in transcript). iPhone: github.com/settings/tokens?type=beta → tap token name → Delete (Delete = revoke the key, touches no files).
- **En dashes remain** in number ranges (150-250, 12-18, Papers II-IV). Correct typography, left in. Say if you want them gone.

## PENDING — SYSTEM

- **Drive copy of `tsp-intake.html` lags at v1** (pre-Menard). Drive's write code-action erred repeatedly this session (service, not size; even a tiny pointer failed). Repo is canon and correct. Retry the Drive write or drop a pointer when the action recovers. NOT a data risk.
- **preship-fidelity-gate.py** (THE WAY) still unbuilt. Blocked on the Convening emitting structured brief.json per build — a founder-in-room session.

## NEXT SESSION OWES A BUILD
Governance shipped this session (fidelity gate spec, Menard). Per spec-rich/build-poor law: next session is a player-facing build, not more governance. Candidate: preship-fidelity-gate.py made real, or a game slice run THROUGH the new intake->brief->build path to prove the loop.

## LANE NOTES
- Repo is authoritative home. Raw CDN caches ~5 min and WILL serve stale — verify via git tree read at commit ref, never the raw URL, right after a push.
- Zapier GitHub write is functional-exact; git-tree readback at the commit ref is the real POST-TICK.
