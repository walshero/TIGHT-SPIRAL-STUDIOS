# HANDOFF — 2026-07-21 (the firefighting chat — collapse-decision trainer)

> **How this handoff was made.** The originating chat ("firefighting") filled and would not
> let a handoff run. This document was reconstructed **from the repo, not the chat** — every
> claim traces to a committed file at a named commit. Nothing here is recalled from the
> conversation; nothing is invented. The durable ledger is the repo (per the omnibus rule),
> so a chat that can't speak for itself is spoken for by what it committed.

## WHAT SHIPPED THIS SESSION (all repo-verified against git tree)

1. **The collapse-decision trainer — first playable case.** `collapse-case-champlain.html`,
   commit `4590463`. A single-file, offline, private game: *Under Load* — Champlain Towers
   South / Surfside, 1:22 a.m., June 24 2021. Home → **3 decisions** (leaning-wall size-up →
   debris-supported void → multi-crew coordination) → per-decision **calibration** (scores
   confidence-to-evidence, not luck) → reviewer-feedback **export block**. Reviewer of record:
   **Ben Urwin — Montreal firefighter & lieutenant.** Engine v1.
   - **Honest-by-design gaps:** load-bearing figures are rendered as **slots, not numbers** —
     5 `PENDING-ENGINEER`, 2 `PENDING-COORDINATOR`. The case does not invent a probability or a
     shore capacity; it names the seat that owes it. This is the Engineer/Coordinator discipline,
     working as specified.

2. **The Pile Panel — the domain convening that governs each case.** `collapse-panel-spec.md`,
   commit `8823c60`. Seven seats (Engineer, Hand, Coordinator, Registrar, Calibrator, Stranger,
   Aleph), ordered by when they speak on a case. Per-game, not studio furniture. Any seat HALTs
   the case; the panel never invents the missing fact, it names who owes it. First Hand-seat
   fill: Ben Urwin.

3. **Studio Eyes, two layers — the architecture this is the first instance of.**
   `studio-eyes-two-layers.md`, commit `de59277`. Layer 1 = universal render/accessibility floor
   (game-agnostic). Layer 2 = per-game domain judgment (the Pile Panel). The Hand seat's review
   of case #1 becomes attributed, dated, reusable heuristics for case #2 — the panel grows and
   carries the reviewer's name.

## PENDING — SYSTEM (blocks the ship; each is bounded and verifiable)

- **The game fails our own gate — 3 H-HUE HALTs (warm mode).** `preship-gate-v4.py
  collapse-case-champlain.html` exits with HALTs: warm-mode text tokens `--dusk` (#f0b877),
  `--ink` (#f6ecda), `--ink2` (#e4d8c2) are **warm near-white** — the "warm-mode ink gold"
  defect `CORPUS-SWEEP-2026-07-19.md` named as the house-style bug the RP-vision Stranger seat
  cannot read. **Default mode is clean AAA; only warm/comfort mode HALTs.** Fix = cool those
  three tokens to near-white (raise blue toward R=G=B), keep amber for fills/accents only. This
  is the "one palette fix propagated," localized to one file.

- **The game is an orphan.** Not linked from `index.html` or `arcade.html` (arcade currently
  lists: cliche-cowpaths, review-bench, sandbags, the-tell, workshop-wall). Unreachable from the
  front door until wired in.

## PENDING — MATT / REAL HUMANS ONLY (cannot be authored by the studio or the machine)

- **The three credentialed seats owe real facts before any trainee ship.** The `PENDING-ENGINEER`
  slots (secondary-collapse probability for the leaning wall; rated shore capacity; time-to-shore
  vs. survival window) need a **licensed PE / forensic-collapse engineer**. The `PENDING-COORDINATOR`
  slots (current INSARAG marking specifics) need verification against the **published INSARAG
  guideline**, not memory. The panel spec is explicit: the device self-checks *before* the humans
  arrive; it does not replace them.

- **Ben Urwin's Hand-seat review.** The game's export block is built to capture exactly this —
  play the case, leave verdicts + notes, export, paste back into the project. Each note becomes an
  attributed Hand-seat heuristic. That review has not yet been run/captured.

## NEXT SESSION OWES A BUILD

Per the spec-rich/build-poor law: the two SYSTEM pendings above **are** the build, and they're
player-facing, not governance. Smallest honest next step: **clear the 3 warm-mode HALTs so the
gate exits SHIP, wire the game into the front door, re-run the gate to prove it, push.** That
converts this session's extracted value into a reachable, gate-passing game. (A ready-to-run
finishing pass for this is offered on branch `claude/firefighting-char-inventory-tsp`.)

## LANE NOTES

- Repo is authoritative home. Raw CDN caches ~5 min and serves stale — verify via git-tree read
  at the commit ref right after a push, never the raw URL.
- The firefighting chat's value is **fully preserved in the three commits above.** The chat being
  un-handoff-able cost the *conversation history*, not the *work* — the work shipped before the
  chat filled. Safe to let that chat go.
