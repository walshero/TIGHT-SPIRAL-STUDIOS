name: Studio Belt
# Spoke stub. Mounts and runs the hub's belt. Update the belt in the hub; every repo inherits it.
on6�ush:  pull_request:
  workflow_dispatch:
jobs:
  belt:
    uses: walshero/TIGHT-SPIRAL-STUDIOS/.github/workflows/belt.yml@main
